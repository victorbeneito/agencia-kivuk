# App (Next.js)
app/node_modules
app/.next
app/.env.local
app/.env*.local

# n8n
n8n/.env
n8n/workflows/*.json.bak

# Material de trabajo (creatividades, vídeos). Fuera de git a propósito: un
# binario grande pesa en el historial para siempre, aunque después se borre.
Pomelli/

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
